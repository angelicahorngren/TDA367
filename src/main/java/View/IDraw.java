package View;

import java.awt.*;

/**
 * An interface that gives object an ability to draw
 */
public interface IDraw {

    /**
     * A method to draw something unspecified using Graphics
     * @param g is a Graphics class object
     */
    void draw(Graphics g);
}
